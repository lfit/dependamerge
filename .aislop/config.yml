# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2026 The Linux Foundation

# aislop quality gate configuration.
#
# The tree is clean: a full-tree scan reports zero findings and scores
# 100. The gate is set to match, so any regression fails rather than
# eroding the score a few points at a time.
#
# Getting here took three steps, recorded in issue #414: splitting the
# oversized modules into packages, splitting merge_manager's 97-method
# class across mixins, and decomposing the eleven functions that ran
# past the length limit.
ci:
  failBelow: 100
